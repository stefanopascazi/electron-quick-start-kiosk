appId: com.stefanopascazi.test-app
artifactName: ${name}-${version}-${arch}.${ext}
productName: GPIO

linux:
  synopsis: GPIO
  description: GPIO
  category: Office
  packageCategory: Office
  target:
    #- target: deb
    - target: AppImage
      arch: 
         - armv7l
         #- x64